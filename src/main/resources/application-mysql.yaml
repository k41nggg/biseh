
spring:

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/zhidao?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: 123123123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

# To activate this profile, run with -Dspring.profiles.active=mysql or set SPRING_PROFILES_ACTIVE=mysql
